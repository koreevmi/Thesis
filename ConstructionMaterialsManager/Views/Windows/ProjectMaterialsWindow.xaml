<Window x:Class="ConstructionMaterialsManager.Views.Windows.ProjectMaterialsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Материалы проекта" WindowStartupLocation="CenterScreen"
        Height="560" Width="760" ResizeMode="CanResize" MinHeight="480" MinWidth="680"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Материалы проекта" FontSize="18" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Список материалов, привязанных к проекту"
                       FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="12" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AddMaterialButton" Content="＋ Добавить материал"
                        Style="{StaticResource PrimaryButton}"
                        Click="AddMaterialButton_Click" Margin="0,0,8,0"/>
                <Button x:Name="EditMaterialButton" Content="✎ Изменить"
                        Style="{StaticResource SecondaryButton}"
                        Click="EditMaterialButton_Click" Margin="0,0,8,0"/>
                <Button x:Name="RemoveMaterialButton" Content="✕ Удалить"
                        Style="{StaticResource DangerButton}"
                        Click="RemoveMaterialButton_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <DataGrid x:Name="ProjectMaterialsDataGrid" Style="{StaticResource ModernDataGrid}"
                      AutoGenerateColumns="False">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}"/>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource ModernDataGridRow}"/>
                    <Style TargetType="DataGridCell" BasedOn="{StaticResource ModernDataGridCell}"/>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Материал" Binding="{Binding Material.Name}" Width="*"/>
                    <DataGridTextColumn Header="Планируемое кол-во" Binding="{Binding PlannedQuantity, StringFormat={}{0:N2}}" Width="Auto"/>
                    <DataGridTextColumn Header="Использовано" Binding="{Binding UsedQuantity, StringFormat={}{0:N2}}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CloseButton" Content="Закрыть"
                    Style="{StaticResource SecondaryButton}"
                    Click="CloseButton_Click" Width="120"/>
        </StackPanel>
    </Grid>
</Window>
